--- a/include/text_strings.h.in
+++ b/include/text_strings.h.in
@@ -223,6 +223,7 @@
 #define TEXT_EXIT_GAME _("ゲームをしゅうりょうする")
 #define TEXT_EXIT_TO_LOBBY _("EXIT TO LOBBY")
 #define TEXT_EXIT_COURSE _("コースからでる？")
+#define TEXT_EXIT_ALL_COURSES _("コースをぜんぶでる？")
 #define TEXT_CAMERA_ANGLE_R _("Ｒボタンのカメラきりかえ")
 
 // Camera Options
@@ -323,6 +324,7 @@
 #define TEXT_CONTINUE _("CONTINUE")
 #define TEXT_EXIT_TO_LOBBY _("EXIT TO LOBBY")
 #define TEXT_EXIT_COURSE _("EXIT COURSE")
+#define TEXT_EXIT_ALL_COURSES _("EXIT ALL COURSES")
 #define TEXT_EXIT_GAME _("EXIT GAME")
 #ifndef VERSION_EU // "R" text is different in EU
 #define TEXT_CAMERA_ANGLE_R _("SET CAMERA ANGLE WITH R")
@@ -470,6 +472,7 @@
 #define TEXT_CONTINUE_FR                _("CONTINUER")
 #define TEXT_EXIT_TO_LOBBY_FR           _("QUITTER AU HALL")
 #define TEXT_EXIT_COURSE_FR             _("QUITTER NIVEAU")
+#define TEXT_EXIT_ALL_COURSES_FR        _("TOUT QUITTER")
 #define TEXT_EXIT_GAME_FR		_("QUITTER JEU")
 #define TEXT_CAMERA_ANGLE_R_FR          _("RÉGLAGE CAMÉRA AVEC [R]")
 // German
@@ -478,6 +481,7 @@
 #define TEXT_CONTINUE_DE                _("WEITER")
 #define TEXT_EXIT_TO_LOBBY_DE           _("ZUR HALLE")
 #define TEXT_EXIT_COURSE_DE             _("KURS VERLASSEN")
+#define TEXT_EXIT_ALL_COURSES_DE        _("ALLE KURSE VERLASSEN")
 #define TEXT_EXIT_GAME_DE		_("SPIEL VERLASSEN")
 #define TEXT_CAMERA_ANGLE_R_DE          _("KAMERA MIT [R] VERSTELLEN")
 
--- a/src/game/level_update.h
+++ b/src/game/level_update.h
@@ -25,6 +25,11 @@
 #define WARP_OP_CREDITS_START 0x17
 #define WARP_OP_CREDITS_NEXT  0x18
 #define WARP_OP_DEMO_END      0x19
+// Pause-menu "Exit All Courses": like WARP_OP_STAR_EXIT, but unwinds every
+// nested entrance in the AP return stack instead of just the most recent one.
+// Only ever set transiently by play_mode_paused() right before initiate_warp(),
+// so it never flows through level_trigger_warp()'s delayed-warp machinery.
+#define WARP_OP_STAR_EXIT_CHAIN_START 0x1A
 
 #define WARP_OP_TRIGGERS_LEVEL_SELECT 0x10
 
--- a/src/game/ingame_menu.c
+++ b/src/game/ingame_menu.c
@@ -2383,25 +2383,34 @@
         { TEXT_EXIT_COURSE_FR },
         { TEXT_EXIT_COURSE_DE }
     };
+    u8 textExitAllCourses[][24] = {
+        { TEXT_EXIT_ALL_COURSES },
+        { TEXT_EXIT_ALL_COURSES_FR },
+        { TEXT_EXIT_ALL_COURSES_DE }
+    };
 
     u8 textCameraAngleR[][24] = {
         { TEXT_CAMERA_ANGLE_R },
         { TEXT_CAMERA_ANGLE_R_FR },
         { TEXT_CAMERA_ANGLE_R_DE }
     };
-#define textContinue     textContinue[gInGameLanguage]
-#define textExitToLobby  textExitToLobby[gInGameLanguage]
-#define textExitCourse   textExitCourse[gInGameLanguage]
-#define textCameraAngleR textCameraAngleR[gInGameLanguage]
+#define textContinue      textContinue[gInGameLanguage]
+#define textExitToLobby   textExitToLobby[gInGameLanguage]
+#define textExitCourse    textExitCourse[gInGameLanguage]
+#define textExitAllCourses textExitAllCourses[gInGameLanguage]
+#define textCameraAngleR  textCameraAngleR[gInGameLanguage]
 #else
     u8 textContinue[] = { TEXT_CONTINUE };
     u8 textExitToLobby[] = { TEXT_EXIT_TO_LOBBY };
     u8 textExitCourse[] = { TEXT_EXIT_COURSE };
+    u8 textExitAllCourses[] = { TEXT_EXIT_ALL_COURSES };
     u8 textCameraAngleR[] = { TEXT_CAMERA_ANGLE_R };
 #endif
     u8 textLevelItems[] = { TEXT_LEVEL_ITEMS };
 
-    handle_menu_scrolling(MENU_SCROLL_VERTICAL, index, 1, exitsAvailable ? 5 : 3);
+    // Rows, when exitsAvailable: 1 Continue, 2 Level Items, 3 Exit Course,
+    // 4 Exit All Courses, 5 Exit to Lobby, 6 Camera Angle.
+    handle_menu_scrolling(MENU_SCROLL_VERTICAL, index, 1, exitsAvailable ? 6 : 3);
 
     gSPDisplayList(gDisplayListHead++, dl_ia_text_begin);
     gDPSetEnvColor(gDisplayListHead++, 255, 255, 255, gDialogTextAlpha);
@@ -2422,10 +2431,11 @@
         } else {
             render_pause_camera_options(x - 42, y - 42, &gDialogCameraAngleIndex, 110);
         }
-    } else if (index[0] != 5) {
+    } else if (index[0] != 6) {
         print_generic_string(x + 10, y - 33, textExitCourse);
-        print_generic_string(x + 10, y - 48, textExitToLobby);
-        print_generic_string(x + 10, y - 63, textCameraAngleR);
+        print_generic_string(x + 10, y - 48, textExitAllCourses);
+        print_generic_string(x + 10, y - 63, textExitToLobby);
+        print_generic_string(x + 10, y - 78, textCameraAngleR);
         gSPDisplayList(gDisplayListHead++, dl_ia_text_end);
 
         create_dl_translation_matrix(MENU_MTX_PUSH, x - X_VAL8, (y - ((index[0] - 1) * yIndex)) - Y_VAL8, 0);
@@ -2435,9 +2445,10 @@
         gSPPopMatrix(gDisplayListHead++, G_MTX_MODELVIEW);
     } else {
         print_generic_string(x + 10, y - 33, textExitCourse);
-        print_generic_string(x + 10, y - 48, textExitToLobby);
+        print_generic_string(x + 10, y - 48, textExitAllCourses);
+        print_generic_string(x + 10, y - 63, textExitToLobby);
         gSPDisplayList(gDisplayListHead++, dl_ia_text_end);
-        render_pause_camera_options(x - 42, y - 72, &gDialogCameraAngleIndex, 110);
+        render_pause_camera_options(x - 42, y - 87, &gDialogCameraAngleIndex, 110);
     }
 }
 
@@ -3666,9 +3677,11 @@
                 gMenuMode = -1;
 
                 if (exitsAvailable && gDialogLineNum == 3) {
-                    num = 4;
+                    num = 4; // Exit Course: back to the previous entrance
                 } else if (exitsAvailable && gDialogLineNum == 4) {
-                    num = 2;
+                    num = 5; // Exit All Courses: back to the start of the chain
+                } else if (exitsAvailable && gDialogLineNum == 5) {
+                    num = 2; // Exit to Lobby: back to the Castle
                 } else {
                     num = 1;
                 }
--- a/src/game/level_update.c
+++ b/src/game/level_update.c
@@ -1205,6 +1205,42 @@
                 gSavedCourseNum = COURSE_NONE;
             }
         }
+    } else if (gPauseScreenMode == 5) {
+        // "Exit All Courses": like gPauseScreenMode 4, but unwinds every
+        // nested entrance in the chain at once instead of just the last one
+        // (0 -> 1 -> 2 -> 3 goes straight back to 0, not to 2).
+        if (gDebugLevelSelect) {
+            fade_into_special_warp(-9, 1);
+        } else {
+            exitWarpNodeId = WARP_NODE_F0;
+            warpNode = area_get_warp_node(exitWarpNodeId);
+            if (warpNode == NULL) {
+                exitWarpNodeId = WARP_NODE_DEATH;
+                warpNode = area_get_warp_node(exitWarpNodeId);
+            }
+            if (warpNode != NULL) {
+                previousDelayedWarpOp = sDelayedWarpOp;
+                gPauseExitCourseSkipDoneScreen = TRUE;
+                sSM64APReturnStyleOverride = SM64AP_RETURN_STYLE_GROUND;
+                if (gPlayer1Controller->buttonDown & L_TRIG) {
+                    sSM64APReturnStyleOverride = SM64AP_RETURN_STYLE_DEATH;
+                }
+                sDelayedWarpOp = WARP_OP_STAR_EXIT_CHAIN_START;
+                // The F1 node supplies a destination for stages without F0, but this is still a
+                // course exit rather than a death warp. Area rando uses the source ID to distinguish them.
+                sSourceWarpNodeId = WARP_NODE_F0;
+                initiate_warp(warpNode->node.destLevel & 0x7F, warpNode->node.destArea,
+                              warpNode->node.destNode, 0);
+                sSM64APReturnStyleOverride = SM64AP_RETURN_STYLE_AUTO;
+                sDelayedWarpOp = previousDelayedWarpOp;
+                fade_into_special_warp(0, 0);
+                gSavedCourseNum = COURSE_NONE;
+            } else {
+                initiate_warp(LEVEL_CASTLE, 1, 0x1F, 0);
+                fade_into_special_warp(0, 0);
+                gSavedCourseNum = COURSE_NONE;
+            }
+        }
     } else if (gPauseScreenMode == 3) {
         // We should only be getting "int 3" to here
         initiate_warp(LEVEL_CASTLE, 1, 0x1F, 0);
--- a/src/sm64ap.cpp
+++ b/src/sm64ap.cpp
@@ -3106,12 +3106,36 @@
     bool isDeathWarp, int warpOp, int returnStyleOverride
 ) {
     bool isReturnWarp = returnStyleOverride != SM64AP_RETURN_STYLE_AUTO
-        || isDeathWarp || warpOp == WARP_OP_DEATH || warpOp == WARP_OP_STAR_EXIT;
+        || isDeathWarp || warpOp == WARP_OP_DEATH || warpOp == WARP_OP_STAR_EXIT
+        || warpOp == WARP_OP_STAR_EXIT_CHAIN_START;
     if (!isReturnWarp || sm64_return_stack.empty()) {
         return false;
     }
 
     int returnStyle = SM64AP_ResolveReturnStyle(isDeathWarp, warpOp, returnStyleOverride);
+    if (warpOp == WARP_OP_STAR_EXIT_CHAIN_START) {
+        // "Exit All Courses" unwinds every nested entrance at once (0 -> 1 ->
+        // 2 -> 3 collapses straight back to 0) instead of one at a time, so
+        // every point being discarded still needs its side effects applied.
+        for (const SM64APReturnPoint &stackPoint : sm64_return_stack) {
+            if (stackPoint.sourceId == 9) {
+                sm64_keep_ssl_pyramid_top_open = true;
+            } else if (stackPoint.sourceId == 10) {
+                sm64_keep_thi_wiggler_entrance_open = true;
+            }
+        }
+        sm64_return_stack.clear();
+        // The oldest entry on this stack is only the return point for the
+        // innermost course-to-course hop (e.g. Chimney -> BITDW). It does not
+        // cover the very first hop into the chain (e.g. the JRB painting
+        // itself), which is tracked separately via sm64_castle_exit_return_active
+        // and is only ever applied when this stack is empty. Returning false
+        // here (instead of resolving to that oldest entry) lets the normal
+        // castle-exit fallthrough below run and finish unwinding that last,
+        // outermost hop too.
+        return false;
+    }
+
     SM64APReturnPoint point = sm64_return_stack.back();
     sm64_return_stack.pop_back();
     if (point.sourceId == 9) {
